{
  "name": "111",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "postinstall": "npx puppeteer browsers install chrome || npx puppeteer browsers install chromium || echo 'Browser install failed, continuing...'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.10.0",
    "input": "^1.0.1",
    "multer": "^2.0.2",
    "puppeteer": "^24.14.0",
    "telegram": "^2.26.22"
  }
}
